// src/components/Header.tsx
import React from 'react';
import Navbar from './Navbar'; // Importar o componente Navbar

const Header: React.FC = () => {
  return (
    <header>
      <Navbar /> 
    </header>
  );
};

export default Header;
